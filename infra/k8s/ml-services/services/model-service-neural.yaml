apiVersion: v1
kind: Service
metadata:
  name: model-service-neural
  labels:
    app: model-service-neural
spec:
  type: LoadBalancer
  ports:
  - port: 8012
    targetPort: 8000
  selector:
    app: model-service-neural