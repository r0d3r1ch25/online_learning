apiVersion: v1
kind: Service
metadata:
  name: model-service-ridge
  labels:
    app: model-service-ridge
spec:
  type: LoadBalancer
  ports:
  - port: 8010
    targetPort: 8000
  selector:
    app: model-service-ridge